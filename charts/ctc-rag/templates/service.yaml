apiVersion: v1
kind: Service
metadata:
  name: {{ include "ctc-rag.fullname" . }}
  labels:
    {{- include "ctc-rag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ctc-rag.selectorLabels" . | nindent 4 }}
